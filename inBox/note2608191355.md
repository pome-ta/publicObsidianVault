```js
const seq = new Tone.Sequence( (time, noteNum) => {
  console.log(`${noteNum}`);
  click.triggerAttackRelease(`A${noteNum}`, '32n', time);
}, [5, 4, 4, 4], '4n', ).start(0); Tone.getTransport().start();
``` 


現在、変数`seq` を定義して、`.start(0)` としている。また、`Tone.getTransport().start();` も呼んでいる。 これで、無事に動いている。

◎ `◎`
s `s`
そこで、
`.start(0)` と、`Tone.getTransport().start()` の関係性や、`Tone.getTransport()` の、`start`/`stop`/`pause` について知りたい。

現在、メトロノームみたいなのを作ってるのだが、

1. 再生
2. （実行中）
3. 停止

という、流れで、再度「1. 再生」をすると、途中からではなく、頭出しで再生をしたい場合には、どのようにすればいいか？


つまり、変数`seq` は、何もせずに、`Tone.getTransport().start();` と、`Tone.getTransport().stop();` のみでいいってこと？




`const plyBtn = p.createButton('ぼたん')` 定義。`plyBtn.mouseReleased` で、押された時の処理定義するけど、

`addEventListener`  よりも、推奨されてるのかな？

`pointerdown` の場合ってどうしたらいい？

`pointerup` は？