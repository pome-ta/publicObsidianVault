結論から。

p5.js の方に赤波線が出てる。


p5.sound の読み込ませる位置を

```js
    for (const { path, text } of results) {
      this.createVirtualFile(path, text);
    }

```


```js
    this.#setupATA();
    this.#ata(`import 'p5';`);
```

この、`ATA` 処理位置で変えてみたけど、

`ATA` 前↓

```
Property 'setup' does not exist on type 'typeof p5'.
```


`ATA` 後↓

```
Property 'setup' does not exist on type 'typeof import("file:///node_modules/p5/types/p5")'.
```


エラーの出方が違う。


また、p5.sound 補完に関しても、出るものと出ないものがある

出ない
- (`p.`) `loadSound`
- `getAudioContext`
- `Oscillator`
- `AudioIn`

出る（`p.` も、`p5.` も）
- `FFT`
- `EQ`
- `p5soundNode`
- `p5soundSource`
- `MonoSynth`

全て試して検証はできてないけど、こんな感じ。


検証用で呼び出す関数やらクラスある？`p.` や`p5.` や`new` とか。指定あれば。

